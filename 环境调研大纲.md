# 分布式系统性能分析体系建设：第一阶段调研报告

**—— 环境画像、全链路工具选型与基准建立**

## 1. 概述 (Executive Summary)

* **1.1 课题背景**
* 针对当前团队在分布式系统性能排查中存在的“经验依赖严重、定位周期长、工具碎片化”痛点，旨在构建一套标准化的性能分析方法论与工具链。


* **1.2 阶段目标**
* 完成目标机器（Hygon + openEuler）的软硬件环境深度摸底。
* 确立适配国产化硬件与 OS 的性能分析工具栈。
* 建立覆盖 **计算、存储（Local/NAS）、网络、数据库交互** 四大维度的实验基准（Baseline）。


* **1.3 核心结论预览**
* **环境就绪**：HygonGenuine (Zen 1) + KVM vPMU 透传已确认，具备硬件级观测能力；openEuler 2.0 (Linux 5.10) 支持 eBPF CO-RE，利于低开销追踪。
* **策略调整**：针对分布式场景，重点新增 **Database 交互分析** 与 **NAS 远程存储分析**；鉴于 Hygon 架构，剔除 Intel VTune，确立以 **perf + eBPF** 为核心的工具路线。



## 2. 目标环境画像 (Target Environment Profile)

*本章节界定物理边界与软件栈，识别架构特性带来的分析重点。*

* **2.1 硬件架构特征**
* **CPU 身份**：
* Vendor ID: `HygonGenuine` (海光)。
* CPU Family: `23` (对应 AMD **Zen 1** 架构)。
* 架构隐患：**CCX (Core Complex) 结构**。
* *分析重点*：需关注跨 CCX 线程调度导致的 L3 Cache Miss 及通信延迟；NUMA 节点数为 1 简化了内存管理，但仍需关注 CPU 亲和性。


* **虚拟化层**：
* Hypervisor: KVM。
* PMU 状态：已验证支持 vPMU 透传（Host 到 Guest 的硬件计数器可读），无“虚拟化黑盒”效应。




* **2.2 操作系统环境**
* **OS 版本**：openEuler 2.0。
* **内核版本**：Linux Kernel 5.10 LTS。
* *优势*：原生支持 `io_uring`（高性能 IO）和更完善的 BPF Type Format (BTF)，为高级 eBPF 工具提供基础。


* **权限策略**：
* 开发/实验环境配置：`kernel.perf_event_paranoid = 0` (允许内核追踪)。
* 符号表策略：`kernel.kptr_restrict = 0` (允许查看内核函数地址)。





## 3. 工具链选型矩阵 (Toolchain Matrix)

*基于 Hygon 兼容性与 openEuler 5.10 特性定制。*

| 分析维度 | 首选核心工具 | 辅助/验证工具 | 针对 Hygon/openEuler 的适配说明 |
| --- | --- | --- | --- |
| **CPU / 微架构** | **perf** + FlameGraph | AMD uProf (待测) | `perf` 原生支持 Hygon PMU；重点分析 IPC 和 Cache Miss。 |
| **内存与堆栈** | **gperftools** (Heap) | Valgrind (Memcheck) | 生产环境用 gperftools 采样；开发环境用 ASan/Valgrind 定位泄露。 |
| **网络 I/O** | **sar -n** / **eBPF** | tcpdump / Wireshark | Linux 5.10 支持 eBPF (tcpstates, tcprtt) 精细化追踪 TCP 状态与重传。 |
| **存储 I/O** | **biosnoop** (eBPF) | iostat / blktrace | 使用 biosnoop 可精确观测单次 IO 延迟（Block 层）。 |
| **NAS / NFS** | **nfsiostat** | mountstats | **新增**：区分 NFS 客户端排队耗时 (avg_exe) 与 服务端响应耗时 (avg_rtt)。 |
| **数据库交互** | **eBPF (dbstat)** | tcpdump | **新增**：通过观测 MySQL/PG 协议响应时间，界定 DB 瓶颈。 |
| **系统调用** | **perf trace** | strace | **策略**：生产环境禁用 strace，使用 perf trace 降低开销。 |

## 4. 实验基准构建 (Baseline Methodology)

*本章节定义五大核心场景的“标准实验”与“观测指标”。*

### 4.1 数据库交互基准 (Database Interaction)

* **场景 A：连接池瓶颈模拟**
* *实验*：高并发请求下限制连接池大小。
* *观测*：Off-CPU 火焰图显示线程阻塞在 `ConditionVariable` 或 `Lock`，而非 `read/write` 系统调用。


* **场景 B：网络传输 vs DB 处理慢**
* *实验*：分别注入网络延迟和慢 SQL。
* *观测*：对比 Application `send` 到 `recv` 的时间差，与 DB 端的执行时间差。


* **场景 C：大数据量反序列化**
* *实验*：大结果集查询（10MB+）。
* *观测*：CPU 热点出现在 JSON/Protobuf 反序列化函数，而非内核态网络接收。



### 4.2 存储 I/O 基准 (Local vs NAS)

* **场景 A：本地 IOPS 极限**
* *实验*：`fio` 压测本地 SSD（随机读写）。
* *观测*：`iostat` await 指标与硬件标称值的对比。


* **场景 B：NAS 远程写入与 Attribute Cache**
* *实验*：多线程高频写入小文件至 NFS 挂载点。
* *观测*：监控 `getattr` 调用频率（属性缓存失效），区分网络带宽争抢与 NFS Server 处理延迟。



### 4.3 网络 I/O 基准 (Distributed Core)

* **场景 A：TCP Incast (多对一并发)**
* *实验*：多 Client 同时向 Server 发送数据。
* *观测*：检查 Server 端软中断 (`ksoftirqd`) 分布，验证 Hygon 多核是否均衡处理中断，有无单核打满丢包。


* **场景 B：网络抖动分析**
* *实验*：Ping-Pong 模型下的长尾延迟捕捉。
* *观测*：使用 `tcpretrans` 和 `tcprtt` 建立正常网络环境下的 RTT 基线。



### 4.4 CPU 与微架构基准 (Zen 1 专项)

* **场景：跨 CCX 数据竞争**
* *实验*：多线程频繁读写共享变量（False Sharing）。
* *观测*：使用 `perf c2c` 观测 `HITM` (Hit Modified) 事件，验证跨核通信的高昂代价。



### 4.5 内存与锁基准

* **场景**：内存泄漏与死锁。
* *观测*：验证 gperftools 在 openEuler 上的符号解析能力；验证 `perf sched` 捕捉锁等待时长的准确性。

## 5. 实施风险与规避 (Risk Assessment)

* **5.1 数据库加密流量不可见**
* *风险*：若应用开启 SSL 连接 DB，tcpdump 无法分析 SQL 语句。
* *对策*：侧重于 eBPF uprobe (挂载到 SSL 解密后的函数) 或仅基于 FD 分析 I/O 延迟。


* **5.2 openEuler eBPF 环境依赖**
* *风险*：openEuler 内核头文件路径可能非标准，导致 BCC 编译失败。
* *对策*：优先使用基于 CO-RE (libbpf) 的工具，摆脱对本地 Kernel Headers 的强依赖。


* **5.3 NAS 环境波动干扰**
* *风险*：NAS 性能受共享网络影响大，基准测试难以复现。
* *对策*：建立“纯净对照组”（本地搭建 NFS Server 模拟），先排除外部网络干扰。



## 6. 下一步工作计划 (Execution Plan)

1. **环境配置 (T+1)**：
* 配置 openEuler yum 源，安装 `perf`, `bcc-tools`, `bpftrace`, `nfs-utils`。
* 调整内核参数 (`paranoid=0`)。


2. **Mock 环境搭建 (T+3)**：
* 部署 MySQL/PostgreSQL 容器与本地 NFS Server。
* **开发基准测试工具**：编写 C++ 程序，具备“连接池耗尽”、“跨核竞争”、“NAS 高频写”等可控开关。


3. **数据采集与手册编制 (T+5)**：
* 执行全场景测试，记录各类瓶颈下的指标特征。
* 输出《Hygon + openEuler 性能特征图谱》。



---

### [附录] 关键检查命令速查表

*(供评审参考，验证技术可行性)*

* **检查 CPU 拓扑**：`lscpu -e` (查看 Core 与 Socket/Node 对应关系)
* **检查 NFS 延迟**：`nfsiostat 1` (分析 Client 端排队)
* **检查 TCP 重传**：`tcpretrans` (eBPF 工具)
* **检查 DB 慢查询**：`dbstat mysql -p PID` (eBPF 工具)
* **检查 PMU 事件**：`perf list | grep -E 'cycles|cache-misses'`
